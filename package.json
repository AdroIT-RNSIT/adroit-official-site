{
  "name": "adroit-official-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"FRONTEND,BACKEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix frontend\" \"npm run backend\"",
    "backend": "cd ai-service && uvicorn main:app --host 0.0.0.0 --port 5000 --reload",
    "install:all": "npm install && npm install --prefix frontend && pip install -r ai-service/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
